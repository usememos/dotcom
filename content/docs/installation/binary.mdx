---
title: Binary Installation  
description: Install Memos using pre-compiled binary releases.
---

Binary installation gives you direct control over the Memos process and is ideal for production deployments.

## Download

We recommend using [memos-builds](https://github.com/memospot/memos-builds) for pre-compiled binaries:

- Cross-platform: Linux, macOS, and Windows
- Multiple architectures: amd64, arm64, and more
- Consistent versioning with checksums

Download the latest release from the [memos-builds releases page](https://github.com/memospot/memos-builds/releases).

## Install

```bash
# Extract the archive
tar -xzf memos_*.tar.gz

# Move to system location
sudo mv memos /usr/local/bin/
sudo chmod +x /usr/local/bin/memos

# Create data directory
sudo mkdir -p /var/lib/memos
```

## Run

### Quick Start

```bash
memos --mode prod --port 5230 --data /var/lib/memos
```

### As a Service (systemd)

```bash
# Create service file
sudo tee /etc/systemd/system/memos.service << 'EOF'
[Unit]
Description=Memos
After=network.target

[Service]
Type=simple
ExecStart=/usr/local/bin/memos --mode prod --port 5230 --data /var/lib/memos
Restart=always
RestartSec=3

[Install]
WantedBy=multi-user.target
EOF

# Enable and start
sudo systemctl daemon-reload
sudo systemctl enable --now memos
```

## Update

1. Download the latest release from [memos-builds](https://github.com/memospot/memos-builds/releases)
2. Replace the binary:

```bash
sudo systemctl stop memos
sudo cp /usr/local/bin/memos /usr/local/bin/memos.backup
tar -xzf memos_*.tar.gz
sudo mv memos /usr/local/bin/
sudo systemctl start memos
```

## Next Steps

- [Configure your instance](/docs/configuration) for databases, authentication, and more
- Set up a reverse proxy for HTTPS access